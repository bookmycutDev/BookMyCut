<UserControl x:Class="BookMyCut.Views.ServiceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="NOS PRESTATIONS" FontSize="32" FontWeight="Bold" 
                   Foreground="#E0B03D" Margin="10,0,0,25"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Services}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#4D000000" CornerRadius="15" Margin="10" Width="250" Padding="20">
                            <StackPanel>
                                <TextBlock Text="{Binding Nom}" FontSize="18" FontWeight="Bold" Foreground="White" TextWrapping="Wrap"/>

                                <Separator Background="#33FFFFFF" Margin="0,10"/>

                                <TextBlock Text="{Binding Prix, StringFormat='{}{0:C}'}" FontSize="20" Foreground="#E0B03D" FontWeight="Bold"/>

                                <StackPanel Orientation="Horizontal" Margin="0,5">
                                    <TextBlock Text="🕒" Margin="0,0,5,0"/>
                                    <TextBlock Text="{Binding DureeMinutes, StringFormat='{}{0} min'}" Foreground="#AAAAAA"/>
                                </StackPanel>

                                <TextBlock Text="{Binding Description}" Foreground="#CCCCCC" TextWrapping="Wrap" 
                                           Margin="0,10" Height="60" FontStyle="Italic" FontSize="12"/>

                                <Button Content="RÉSERVER" 
                                        Command="{Binding DataContext.ReserverServiceCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Background="#E86C1A" Foreground="White" FontWeight="Bold" Height="40">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="8"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>